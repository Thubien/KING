{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE"
  },
  "deploy": {
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10,
    "startCommand": "echo 'Starting Laravel...' && php artisan migrate --force && echo 'Migration done' && php artisan config:cache && echo 'Config cached' && php -v && echo 'PHP version ok' && php artisan serve --host=0.0.0.0 --port=$PORT --verbose"
  }
} 